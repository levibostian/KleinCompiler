; File currently not under use. 

;#lang racket
;
;(require "scanner.rkt"
;         "parser.rkt"
;         "print-parser.rkt")
;
;(define function-name
;  (vector-ref (current-command-line-arguments) 1))
;
;(define file-name
;  (vector-ref (current-command-line-arguments) 2))
;
;(function-name file-name)

